# Credentials for the self-hosted Supabase stack (infra/supabase-lite/).
# The URL is the domain in front of its Caddy service; the key is the ANON_KEY
# that infra/supabase-lite/generate-secrets.sh produced — a JWT, not the
# sb_publishable_… form the hosted project used.
VITE_SUPABASE_URL=https://supabase.example.com
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
